import { useState } from "react";
import PageLayout from "../components/layout/PageLayout";
import { submitSellerApplication } from "../services/productService";
import "./CommercePages.css";
export default function SellerApply(){const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function submit(event){event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));setBusy(true);try{await submitSellerApplication(data);event.currentTarget.reset();setMessage("Application received. We will review your farm details soon.");}catch(error){setMessage(error.message);}finally{setBusy(false);}}return <PageLayout><main className="screen narrow"><header className="page-title"><p>GROW WITH MAVIINA MANE</p><h1>Become a farm partner</h1><span>Tell us about your farm and the mangoes you grow.</span></header><form className="panel form-grid" onSubmit={submit}><label>Farm name<input name="farmName" required/></label><label>Your name<input name="applicantName" required/></label><label>Email<input type="email" name="email" required/></label><label>Phone<input name="phone" required/></label><label className="full">Farm address<textarea name="address" required/></label><label className="full">What do you sell?<textarea name="products" placeholder="Varieties, harvest season, pack sizes" required/></label><button className="green-button full" disabled={busy}>{busy?"Submitting...":"Submit application"}</button>{message&&<p className="form-success full">{message}</p>}</form></main></PageLayout>}
